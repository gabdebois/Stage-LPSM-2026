$d\Gamma_x^* = \sum_{\vert n \vert < 2+s} d \pi_x^{(n)} \Gamma_x^* \diff_{z_n}$

$\mathbb{R}[z_{3}, z_{n}]$-endomorphisme.

To construct $\delta \Pi$, we are going to use the reconstruction theorem, by modelling it with the modelled distribution $\{d\Pi_{x}^*\}_{x}$.

